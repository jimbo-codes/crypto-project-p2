// render the full list of data returned from App level fetch
// .map across the array to populate the table.

import React,{useState} from "react";
function CryptoList({}) {
    return (      
        <div>
          List of top coins (table, bleh)
        </div>
      );
    }
    
export default CryptoList;